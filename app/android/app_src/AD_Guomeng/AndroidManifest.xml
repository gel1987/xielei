<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xl.kfpanda"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="8" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name="com.cdd.guomob.BannerActivity" >
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>    
        </activity>
        <!-- Guomeng banner -->
        <activity
            android:name="com.guomob.banner.GuomobAdActivity"
            android:theme="@android:style/Theme.Translucent" />
        <!-- Guomeng banner -->
        <!-- Guomeng cover -->
        <activity
            android:name="com.guomob.screen.GuomobAdActivity"
            android:theme="@android:style/Theme.Translucent" />
        <!-- Guomeng cover -->
        <meta-data
            android:name="guomobid"
            android:value="appid:gfrw0dbyn2s1153" />
    </application>

</manifest>